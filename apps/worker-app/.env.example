# Backend URL — in dev, this is your machine's local LAN IP (not localhost, since it's accessed from a physical phone)
EXPO_PUBLIC_API_URL=http://192.168.x.x:5000

# HyperTrack public key — from https://dashboard.hypertrack.com
EXPO_PUBLIC_HYPERTRACK_PUB_KEY=your_hypertrack_publishable_key_here
